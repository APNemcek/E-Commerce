function toggleMenuLogin() {
    const menu = document.getElementById("loginMenu");
    menu.style.display = (menu.style.display === "block") ? "none" : "block";
}

function toggleMenuCateg() {
    const menu = document.getElementById("categMenu");
    menu.style.display = (menu.style.display === "block") ? "none" : "block";
}

